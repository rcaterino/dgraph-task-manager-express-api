{
  "name": "express-restfull-api",
  "version": "1.0.0",
  "description": "Restfull API with Express in typescript to serve data to frontend from Dgraph database",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node ./dist/app.js"
  },
  "keywords": [
    "typescript",
    "express",
    "restfull",
    "api",
    "dgraph"
  ],
  "author": "Ricardo Fabio Caterino Polini",
  "license": "ISC",
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "dotenv": "^16.3.1",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "express": "4.18.2"
  }
}
